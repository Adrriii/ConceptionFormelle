#include "limits.h"

/* Cette fonction renvoie la plus petite valeur parmi ses arguments.
*/
int min(int a, int b, int c);

/* Cette fonction renvoie a/2 si a est pair et 3*a + 1 sinon.
*/
int syracuseStep(int a);

/* Cette fonction renvoie a/b si a%b <= b/2, et renvoie (a/b)+1 sinon.
*/
int roundedDiv(int a, int b);